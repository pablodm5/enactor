// import the overriding component
// put the component to the map using key as the COMPONENT_KEY of the overriden component
// Ex: If BasketView(having component key as "BasketView") compoent is to be overriden by BasketViewNew component then add 
// BasketView: BasketViewNew to the map

const TrainingPosComponentsMap = {
};

export default TrainingPosComponentsMap;
